({
    /*
	* 	@Method: searchRecord
	*	@param: component,event and helper
	*	@Desc: Called When search Button is clicked
	*/
	searchRecord : function(component, event, helper) {
        helper.searchRecord_helper(component,event);
	}
})